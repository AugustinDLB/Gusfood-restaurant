#ifndef PLAYER_ACTION_HPP
#define PLAYER_ACTION_HPP

// Enum pour les actions logiques (séparé des touches physiques)
enum class PlayerAction
{
  MOVE_LEFT,
  MOVE_RIGHT,
  MOVE_UP,
  MOVE_DOWN
};

#endif

